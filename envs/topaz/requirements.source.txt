# Verbatim upstream dependency sources for Topaz
# Repo: https://github.com/tbepler/topaz
# Pinned commit: 58fe52370f4accb8215525df2ea8f2c7ee6d340a (v0.3.20, 2026-05-11)
#
# --- setup.py / pyproject.toml ---
# name = "topaz-em"
# python_requires = ">=3.8,<=3.13"
# license = GPLv3
# entry_points: console_scripts -> topaz = topaz.main:main
#
# --- requirements.txt (verbatim) ---
torch>=1.0.0
torchvision
tqdm>=4.65.0
h5py>=3.7.0
numpy>=1.11
pandas>=0.20.3
psutil>=5.8.0
scikit-learn>=0.19.0
scipy>=0.17.0
pillow>=6.2.0
future
#
# --- Notes on official install (README) ---
# Official README recommends:  conda install topaz -c tbepler -c pytorch
#   (this pulls pytorch + cudatoolkit from conda).
# Alternative official:        pip install topaz-em
# torch/torchvision CUDA build is NOT pinned upstream (torch>=1.0.0).
